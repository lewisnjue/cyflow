#!/bin/bash

## we will need open blas for this project

# for me because am on debian machine installation is easy:
sudo apt update
sudo apt install libopenblas-dev
sudo apt install -y nvidia-cuda-toolkit build-essential